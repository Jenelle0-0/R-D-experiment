export default function Home() {
  return (
    <div>
      <h1>R-D Experiment — Next.js</h1>
      <p>This repository is now a minimal Next.js application (App Router).</p>
      <p>
        Try visiting <a href="/api/hello">/api/hello</a> to see a JSON response.
      </p>
    </div>
  )
}
